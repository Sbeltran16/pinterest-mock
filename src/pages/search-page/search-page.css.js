import { Grid } from "@mui/material";
import { styled } from "@mui/system";

const StyledPageGrid = styled(Grid)({
  display: "flex",
  flexDirection: "column",
  width: "100%",
  backgroundColor: "#fffd92",
  justifyContent: "center",
  alignItems: "center",
});

export { StyledPageGrid };
